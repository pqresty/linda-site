# Где мы сейчас

Снимок состояния на 16 августа 2026. Обновлять при заметных сдвигах —
это то, с чего проще всего поднять контекст в новой сессии.

## Сайт

Живёт на своём домене: **http://lindaconcerts.ru** — отдаётся GitHub Pages из
ветки `main` репозитория `pqresty/linda-site` (папка `site/`). Исходники — в
ветке `source` того же репозитория, порядок работы описан в `README.md`.

Старый адрес `pqresty.github.io/linda-site/` отдаёт 301 на домен, прежние
ссылки продолжают работать.

**HTTPS ещё не поднялся.** GitHub выпускает сертификат после того, как домен
прописан в `CNAME`; на момент записи `https://lindaconcerts.ru` не отвечает,
`http://` работает. Если через сутки сертификата так и не будет — в настройках
репозитория Settings → Pages снять и заново вписать домен, это перезапускает
выпуск, и проверить галку «Enforce HTTPS».

## DNS

Зона живёт **в панели хостинга** (ISPmanager, аккаунт u3601068), а не в
кабинете REG.RU: у домена NS `ns1/ns2.hosting.reg.ru`. Путь — «Хостинги» →
иконка монитора у «Хостинг Host-0» → «Управление DNS».

Настроено: четыре A на апексе (185.199.108–111.153), CNAME `www` →
`pqresty.github.io.`, записи AAAA сняты. Их обязательно было убрать — иначе
посетители с IPv6 уходили бы на старый хостинг.

Почтовые записи не тронуты: MX на `mx1/mx2.hosting.reg.ru`, поддомены `mail`,
`smtp`, `pop`, `ftp` смотрят на прежний сервер.

## Что осталось сделать

**Ссылка на фотографа.** В подвале стоит «Фотографии — Дарья Красная» со
ссылкой на Instagram. Instagram принадлежит Meta, признанной в России
экстремистской, — при упоминании нужна пометка, за её отсутствие штрафуют по
ч. 2 ст. 13.15 КоАП. Решено было снять ссылку и оставить имя текстом, но
правка ещё не внесена. Площадки вне Instagram у неё не нашлось: `vk.com/
ph.krasnaya` и `vk.com/phkrasnaya` — 404, телеграм-аккаунтов `@ph_krasnaya` и
`@phkrasnaya` не существует (проверено по заголовку страницы: у живого
аккаунта там имя владельца). Стоит спросить у неё ВК или Телеграм — тогда
ссылку можно вернуть без оговорок.

**Почтовый ящик** `booking@lindaconcerts.ru` — стоит на сайте, но не заведён,
письма падают. Заводить в панели «Хостинг Mail-1» (u3341838): «Почта» →
«Почтовые ящики» → создать. MX-записи уже готовы, зону менять не нужно.

**Страницы-примерки** `transitions.html`, `mobile-crop.html`, `scrim.html`
лежат на боевом домене по прямым адресам. Убрать — очистить список `EXTRA` в
`build/deploy.py`.

**Тольятти 8 ноября.** На rolld висит концерт, которого нет: кнопка покупки
ведёт на московское событие того же дня (Тверская). Текст письма в их
поддержку готов — `pismo-rolld.md`, адрес `hi@rolld.ru`. Не отправлено.

**Декабрьские даты** заказчик обещал прислать отдельно. На rolld значится
16 декабря 2026, Москва, Petter, 21:00 — за пределами текущего периода афиши.

**Двенадцать дат стоят со «Скоро»** — весь ноябрь и Казань 25 августа: по ним
нет ссылок и времени. Израиль: продажи открывались 16 августа, ссылка
`showman.co.il/e/linda-2026/` уже вписана, статус пора переключить на
`on_sale`, если билеты появились.

## Чего делать не надо

Не ставить логотип Instagram — демонстрация символики квалифицируется строже
простого упоминания.

Не угадывать поддомены сети «Руки Вверх! Бар»: `khimki.rvbar.ru` существует,
но отдаёт страницу Таганки. Верный список городов — на `ruki.club`.

Не трогать записи MX и почтовые поддомены при работе с зоной.
